name: Deploy Portfolio to GitHub Pages

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm install

    - name: Inject Web3Forms access key
      env:
        WEB3_FORM_ACCESS_KEY: ${{ secrets.WEB3_FORM_ACCESS_KEY }}
      run: |
        python - <<'PY'
        import os
        from pathlib import Path

        path = Path('src/environments/environment.prod.ts')
        text = path.read_text()
        key = os.environ.get('WEB3_FORM_ACCESS_KEY', '')
        text = text.replace("web3FormsAccessKey: ''", f"web3FormsAccessKey: '{key}'")
        path.write_text(text)
        PY
      
    - name: Build application
      run: npm run build
      
    - name: Create .nojekyll and 404.html for SPA routing
      run: |
        touch docs/.nojekyll
        cat > docs/404.html << 'EOF'
        <!doctype html>
        <html lang="en">
        <head>
          <meta charset="utf-8">
          <title>Ankit Sharma - Software Developer</title>
          <base href="/Portfolio/">
          <meta content="width=device-width, initial-scale=1" name="viewport">
          <script>
            var pathSegmentsToKeep = 1;
            var l = window.location;
            l.replace(
              l.protocol + '//' + l.hostname + (l.port ? ':' + l.port : '') +
              l.pathname.split('/').slice(0, 1 + pathSegmentsToKeep).join('/') + '/?/' +
              l.pathname.slice(1).split('/').slice(pathSegmentsToKeep).join('/').replace(/&/g, '~and~') +
              (l.search ? '&' + l.search.slice(1).replace(/&/g, '~and~') : '') +
              l.hash
            );
          </script>
        </head>
        <body></body>
        </html>
        EOF
      
    - name: Setup Pages
      uses: actions/configure-pages@v4
      
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: './docs'
        
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
